media:
  input: .
  output: .

content:
  - name: posts
    label: Posts
    type: collection
    path: posts
    filename: "{year}-{month}-{day}-{primary}.md"
    view:
      fields: [title, date]
    fields:
      - name: title
        label: Title
        type: string
        required: true

      - name: date
        label: Date
        type: date
        required: true

      - name: summary
        label: Summary
        type: text
        required: true

      - name: body
        label: Body
        type: rich-text
        required: true